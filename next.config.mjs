/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["www.google.com", "via.placeholder.com", "www.freepik.com", "img.freepik.com"],
      },
};

export default nextConfig;
